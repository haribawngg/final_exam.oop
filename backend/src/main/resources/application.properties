# ==============================================================
# C?U HÌNH DATABASE MYSQL (QUAN TR?NG NH?T)
# ==============================================================
spring.datasource.url=jdbc:mysql://localhost:3306/edumanager_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# M?t kh?u b?n cung c?p
spring.datasource.password=CSDL2021
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================================================
# C?U HÌNH JPA / HIBERNATE (T? ??NG T?O B?NG)
# ==============================================================
# update: T? ??ng t?o b?ng n?u ch?a có, gi? nguyên d? li?u c?
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Hi?n th? câu l?nh SQL trong Console ?? d? debug l?i
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==============================================================
# C?U HÌNH SERVER & LOGGING
# ==============================================================
server.port=8080

# Log chi ti?t cho project c?a b?n ?? d? soi l?i
logging.level.com.school.grademanagement=DEBUG
# Log chi ti?t cho ph?n b?o m?t (Login)
logging.level.org.springframework.security=DEBUG

# ==============================================================
# C?U HÌNH KHÁC (THYMELEAF - N?u b?n dùng template)
# ==============================================================
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html